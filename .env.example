# PointSet CRM — Environment Variables
# Copy this to .env and change the values

# Server port
PORT=3000

# Login credentials (change these!)
CRM_USER=pointset
CRM_PASS=your-secure-password-here

# Database path (default: ./db/crm.sqlite)
DB_PATH=./db/crm.sqlite
